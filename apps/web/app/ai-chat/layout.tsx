/**
 * AI Chat layout — minimal, no sidebar, no AegisVision nav.
 * This page opens in a new tab as a standalone chat interface.
 */
export default function AIChatLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return <>{children}</>;
}
